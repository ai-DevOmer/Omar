[phases.setup]
nixPkgs = ["python311", "gcc", "rustc", "cargo", "openssl", "pkg-config"]

[phases.install]
cmds = ["pip install uv", "uv sync"]

[start]
cmd = "uv run uvicorn main:app --host 0.0.0.0 --port $PORT"
